server:
  port: 8083

spring:
  application:
    name: learningServer
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  data:
    mongodb:
      uri: ${MONGO_URI}
